# Backend runtime deps (subset of the root requirements.txt). Pinned to exact versions so a rebuilt
# image ships the same set the tests passed against; keep these in sync with requirements.lock.
fastapi==0.138.0
uvicorn[standard]==0.49.0
pandas==2.3.3
pyarrow==24.0.0

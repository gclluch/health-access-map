[pytest]
# The suite imports `pipeline` and `backend` from the repo root. Without this, a bare `pytest`
# (as opposed to `python -m pytest`, which injects the cwd) fails collection on a fresh clone.
pythonpath = .
testpaths = tests
